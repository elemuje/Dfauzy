# D'fauzy Fabrics

Premium fabric e-commerce landing page built with Vite + React + Tailwind CSS.

## Tech Stack

- **Vite 5** — lightning-fast dev server & build
- **React 18** — UI
- **Tailwind CSS 3** — utility-first styling
- **Lucide React** — icons

## Getting Started

```bash
npm install
npm run dev
```

Open [http://localhost:5173](http://localhost:5173) in your browser.

## Build

```bash
npm run build   # outputs to /dist
npm run preview # preview production build locally
```

## Deploy to Vercel

1. Push this repo to GitHub.
2. Go to [vercel.com](https://vercel.com) → **New Project** → import your repo.
3. Vercel auto-detects Vite — just click **Deploy**. No extra config needed.

## Deploy to GitHub Pages (alternative)

Add `"base": "/your-repo-name/"` to `vite.config.js` if hosting on a subpath, then run:

```bash
npm run build
# push /dist to the gh-pages branch
```

## Project Structure

```
dfauzy-fabrics/
├── public/
│   └── images/          # All fabric & CEO photos
├── src/
│   ├── App.jsx          # Main page component
│   ├── main.jsx         # React entry point
│   └── index.css        # Tailwind + custom CSS
├── index.html
├── vite.config.js
├── tailwind.config.js
├── postcss.config.js
└── package.json
```

## Contact

WhatsApp: +234 701 542 5936  
Email: dfauzyventures@gmail.com
